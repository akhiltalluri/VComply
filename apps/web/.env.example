# Browser-visible base URL for API requests. Keep this same-origin in development
# so the frontend can run on port 8000 without colliding with the backend.
NEXT_PUBLIC_API_URL=/api

# Server-side proxy destination for the FastAPI backend (no trailing slash)
BACKEND_API_URL=http://localhost:8001
